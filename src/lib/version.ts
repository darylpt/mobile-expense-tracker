// ponytail: single source of truth for app version.
// Bump this when committing a release or notable change.
// Run `npm run version:bump` to auto-increment the patch number.
export const APP_VERSION = '0.1.0';
