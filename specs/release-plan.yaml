# Release Plan — Hardening Pass (v0.2.16)

> Target: Address 3 medium-severity edge cases from edge-case-analysis.md
> Phase: plan → build → verify → release
> Created: 2026-07-13

---

## Epic: Edge-Case Hardening — ✅ COMPLETED 2026-07-17

### Story 1: Cross-Tab Auth Guard ✅
Already handled by existing implementation.

### Story 2: CSV Import Validation Hardening ✅
- 2a: Duplicate-account transfer check in `parseCsv()` (`csv-import.ts:271`)
- 2b: Negative starting balance warning in `CsvImportPreview.tsx:93-101`
- Tests: duplicate-account error test in `csv-import.test.ts:120`

### Story 3: Pre-Import IDB Quota Check ✅
- `checkStorageQuota()` helper in `csv-import.ts:437`
- Called from `CsvImportPreview.tsx:32` via `useEffect`
- Storage warning banner displayed at `CsvImportPreview.tsx:104-108`
- Tests: 4 cases in `csv-import.test.ts:127-176`

---

## Execution Order

1. **Story 2** (CSV Validation) — two sub-tasks, touches parser + preview component
2. **Story 3** (IDB Quota) — touches parser + preview component, builds on Story 2's preview changes

## Non-Goals

- ✅ Quick Add double-submit guard — fixed 2026-07-15 (belt-and-suspenders guard in handleSubmit)
- ✅ Payout empty-splits guard — fixed 2026-07-15 (canSave requires splits.length > 0)
- ✅ Payout negative amount validation — already handled (totalAmount <= 0 check existed)
- ✅ Transaction list filter+pagination reset — already handled (page param deleted on filter change)

## Success Criteria

- All 42 existing unit tests pass
- New unit tests for each story pass
- 0 ESLint errors, 0 TS errors
- Manual E2E: CSV with duplicate-account transfer shows error in preview
- Manual E2E: import preview shows storage warning when quota is low
